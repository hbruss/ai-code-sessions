# Setup Preflight Command Override Bugfix

- [x] Add a failing test showing setup preflight respects configured CLI command overrides
- [x] Implement the minimal fix so probing uses existing configured command overrides
- [x] Rerun targeted tests and report results
