# Release 0.1.8

- [ ] Confirm working tree clean (besides intended changes)
- [ ] Commit changes and push to `main`
- [ ] Build distributions (`uv build --clear`)
- [ ] Validate artifacts (`twine check dist/*`)
- [ ] Publish to TestPyPI (requires `op` / 1Password approval)
- [ ] Install from TestPyPI in a fresh env and smoke-test `ais --version`
- [ ] Publish to PyPI (requires `op` / 1Password approval)
- [ ] Verify install from PyPI (`pipx install ai-code-sessions --force`)
