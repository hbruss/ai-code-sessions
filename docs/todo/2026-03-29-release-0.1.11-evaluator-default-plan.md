# Release 0.1.11 Evaluator Default Fix Plan

- [x] Add failing tests covering `ais changelog sync` evaluator precedence
- [x] Make `ais changelog sync` honor explicit flag, then env, then config, then `codex`
- [x] Update CLI/help/docs if the new default behavior needs clarification
- [x] Run focused tests for sync evaluator selection
- [x] Run full release verification (`pytest`, Ruff, build, `twine check`)
- [x] Request code review on the patch release candidate
- [x] Bump version from `0.1.10` to `0.1.11`
- [x] Commit and push release commits on `main`
- [x] Publish `0.1.11` to TestPyPI and PyPI
- [x] Smoke test install of `ai-code-sessions==0.1.11`
- [x] Archive build and smoke-test artifacts under `.archive/`

## Notes

- PyPI accepted the upload immediately, but the public simple index lagged briefly before `pip install ai-code-sessions==0.1.11` succeeded. A retry with `--no-cache-dir` passed once propagation completed.
